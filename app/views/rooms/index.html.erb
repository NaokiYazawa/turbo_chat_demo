<div class="w-full h-screen">
  <div class="flex">
    <div class="flex-1 w-full h-full">
      <div class="main-body container m-auto w-11/12 h-screen flex flex-col">
        <div class="main flex-1 flex flex-col">
          <div class="flex-1 flex">
            <div class="sidebar hidden h-screen overflow-y-scroll lg:flex w-1/3 flex-2 flex-col pr-6">
              <%= render "layouts/sidebar", rooms: @rooms %>
            </div>
            <div class="sidebar h-screen overflow-y-scroll flex lg:hidden w-full flex-2 flex-col">
              <%= render "layouts/sidebar", rooms: @rooms %>
            </div>
            <div class="hidden flex-1 lg:flex flex-col justify-center items-center h-screen">
              <p class="text-3xl">select or create a room</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
